import java.util.HashMap;

public class updateContact extends Contacts {
    @Override
    void update(String name, String number,HashMap<String,String> temp) {
        super.update(name, number,temp);
    }
    // updateContact(String name, int number) {
    //     Contacts a=new Contacts();
    //     a.update(name, number);
    // }
}
