import java.util.HashMap;

public class deleteContact extends Contacts{

    @Override
    void delete(String name,HashMap<String,String> temp) {
        super.delete(name,temp);
    }
    

    // deleteContact (String name) {     
    //     Contacts a=new Contacts();
    //     a.delete(name);
    // }
}
